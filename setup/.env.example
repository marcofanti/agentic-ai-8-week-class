# Copy this file into your project as ".env" and replace the values with your
# real keys (they're in your password manager). Then make sure git ignores it:
#     echo ".env" >> .gitignore
# This example file is safe to commit — it holds no real secrets.
ANTHROPIC_API_KEY=sk-ant-your-real-key-here
OPENAI_API_KEY=sk-your-real-key-here
